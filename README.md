# Automate_Login_Test
Selenium Login Automation Test project automates the login functionality of a web application using Selenium WebDriver with Java. The script opens the browser, enters credentials, performs login, and verifies whether the login is successful.
